document.addEventListener("DOMContentLoaded", function() {
    const options = {
      id: 'calculator', // ID элемента, в который будет встроен калькулятор
      theme: 'dark', // Тема (default или dark)
    };
  
    const calculator = new Calculator(options);
  });
  